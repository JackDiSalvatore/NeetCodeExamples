[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-code-examples"
version = "0.1.0"
description = "Python Code Examples"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "black",
    "pytest"
]
