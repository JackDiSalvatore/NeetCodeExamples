import { plusMinusRatios } from "../../../src/hacker_rank/problem_solving/plus-minus-ratios";

describe("plus minus ratios of an array", () => {
  it("calculate the positive, negative, and zero ratios of an array", () => {
    const arr = [1, 1, 0, -1, -1];

    const res = plusMinusRatios(arr);
  });
});
